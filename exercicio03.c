#include <stdio.h>

int main(){
    float base;
    float altura;
    float area;

    printf("Qual o valor da base: \n");
    scanf("%f", &base);

    printf("qual o valor da altura: \n");
    scanf("%f", &altura);

    area = base * altura;

    printf("O valor da area é: %.2f \n", area);

    /*Calculando area de um retangulo e 
    testando novas variaveis.*/
}